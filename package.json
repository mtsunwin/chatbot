{
  "name": "scc-backend",
  "version": "1.0.0",
  "description": "ChatHub",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && nodemon dist/index.js",
    "serve": "npm run build && node dist/index.js",
    "test": "npm run build && mocha --require babel-register"
  },
  "author": "thangtm13@fpt.com.vn",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "loadash": "^1.0.0",
    "morgan": "^1.9.1",
    "rotating-file-stream": "^1.3.9"
  }
}
